[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
# Переходим в backend, добавляем текущую папку в пути поиска Python и запускаем
cmd = "cd backend && PYTHONPATH=. uvicorn main:app --host 0.0.0.0 --port ${PORT}"