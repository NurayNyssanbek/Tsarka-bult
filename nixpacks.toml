[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.install]
# ВНИМАНИЕ: тут .txt в конце, проверь это!
cmds = ["cd backend && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt"]

[start]
cmd = "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port ${PORT}"