[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
# Мы говорим Python, что папка backend является источником модулей
cmd = "PYTHONPATH=$PYTHONPATH:./backend uvicorn backend.main:app --host 0.0.0.0 --port ${PORT}"